language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulp-ionic' ]; then cd .. && eval "mv $currentfolder generator-gulp-ionic" && cd generator-gulp-ionic; fi

